<%= render '/common/header' %>
<h1 class="h2"><%= t('host_type') %></h1>
<% if notice.present? %><div class="alert alert-info"><%= notice %></div><% end %>

<table class="table table-condensed table-striped table-hover table-bordered">
  <tbody>
    <tr>
      <th style="text-align: right;" width="200px"><%= t('host_type_name') %> :</th>
      <td><%= @host_type.name %></td>
    </tr>
    <tr>
      <th style="text-align: right;" width="200px"><%= t('host_type_description') %> :</th>
      <td><%= @host_type.description %></td>
    </tr>
    <tr>
      <th style="text-align: right;" width="200px"><%= t('host_type_hosts') %> :</th>
      <td>
        <% if @host_type.hosts.present? %>
          <table class="table table-condensed table-hover">
            <tr>
              <th style="text-align: center;">#</th>
              <th style="text-align: center;"><%= t('hostname') %></th>
              <th style="text-align: center;"><%= t('infraestructure') %></th>
              <th style="text-align: center;"><%= t('environment') %></th>
              <th style="text-align: center;"><%= t('description') %></th>
            </tr>
            <% counter = 1 %>
            <% @host_type.hosts.each do |host| %>
              <tr>
                <td><%= counter %></td>
                <td><%= link_to host.name, host %></td>
                <td><%= host.infraestructure.name %></td>
                <td><%= host.environment.name %></td>
                <td><%= host.description.html_safe %></td>
              </tr>
              <% counter += 1%>
            <% end %>
          </table>
        <% else %>
          ---
        <% end %>
      </td>
    </tr>
  </tbody>
</table>

<%= link_to t('back'), :back, class: 'btn btn-default' %>
<%= link_to t('edit'), edit_host_type_path(@host_type), class: 'btn btn-primary' %>
<%= link_to t('add'), new_host_type_path, class: 'btn btn-default' %>
