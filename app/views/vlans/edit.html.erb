<%= render layout: "common/admin_resource_form", locals: {
  heading: t("edit_vlan"),
  subheading: "##{@vlan.number} · #{@vlan.network}/#{@vlan.netmask}",
  index_path: vlans_path,
  index_label: t("vlans"),
  current_label: @vlan.name,
  back_path: vlans_path,
  width_class: "max-w-4xl"
} do %>
  <%= render "form", vlan: @vlan %>
<% end %>
