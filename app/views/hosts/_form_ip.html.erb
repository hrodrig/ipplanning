<%= form_for(@ip, url: create_ip_to_host_path, html: {class: "form-horizontal"}) do |form| %>
  <% if ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ip.errors.count, "error") %> prohibited this ip from being saved:</h2>

      <ul>
      <% ip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label t('extras_ips')+':', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= @host.all_ips.html_safe %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :address, t('ip')+':', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= form.text_field :address, id: :ip_name, class: "form-control", placeholder: "Dirección IP" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-2 control-label">&nbsp;</div>
    <div class="col-sm-10">
      <div class="actions">
        <%= form.submit t('ok'), class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
