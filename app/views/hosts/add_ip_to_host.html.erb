<%= render layout: "common/admin_resource_form", locals: {
  heading: "#{t('add_ip_to')}: #{@host.name}",
  index_path: hosts_path,
  index_label: t("hosts"),
  current_label: @host.name,
  back_path: host_path(@host),
  width_class: "max-w-3xl"
} do %>
  <%= render "form_ip", ip: @ip %>
<% end %>
