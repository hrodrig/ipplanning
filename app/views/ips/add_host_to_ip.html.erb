<%= render layout: "common/admin_resource_form", locals: {
  heading: "#{t('add_host_to_ip')}: #{@ip.address}",
  index_path: ips_path,
  index_label: t("ips"),
  current_label: @ip.address.to_s,
  back_path: ips_path,
  width_class: "max-w-3xl"
} do %>
  <%= render "form_host", host: @host %>
<% end %>
