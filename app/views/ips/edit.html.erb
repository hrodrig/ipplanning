<%= render layout: "common/admin_resource_form", locals: {
  heading: t("edit_ip"),
  index_path: ips_path,
  index_label: t("ips"),
  current_label: @ip.address.to_s,
  back_path: ips_path,
  width_class: "max-w-4xl"
} do %>
  <%= render "form", ip: @ip %>
<% end %>
