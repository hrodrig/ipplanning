<%= form_for(@host, url: create_host_to_ip_path, html: {class: "form-horizontal"}) do |form| %>
  <% if host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ip.errors.count, "error") %> prohibited this host from being saved:</h2>

      <ul>
      <% host.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :id %>

  <div class="form-group">
    <%= form.label :name, 'Hostname', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= form.text_field :name, id: :host_name, class: "form-control", placeholder: "Nombre del host" %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :description, 'Notas', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= form.text_area :description, id: :host_description, class: "form-control", placeholder: "Información adicional" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-2 control-label">&nbsp;</div>
    <div class="col-sm-10">
      <div class="actions">
        <%= form.submit "Aceptar", class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
