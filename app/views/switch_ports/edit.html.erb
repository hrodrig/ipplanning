<%= render layout: "common/admin_resource_form", locals: {
  heading: t("editing_switch_port"),
  index_path: network_switch_path(@network_switch),
  index_label: @network_switch.name,
  current_label: @switch_port.name,
  back_path: network_switch_path(@network_switch)
} do %>
  <%= render "form", switch_port: @switch_port %>
<% end %>
